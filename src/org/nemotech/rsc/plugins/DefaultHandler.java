package org.nemotech.rsc.plugins;

/**
 * Used to identify the default handler plugin which represents any action that is not handled in another script.
 * 
 * Usually will return "Nothing interesting happens" or something similar.
 *
 */
public interface DefaultHandler {

}